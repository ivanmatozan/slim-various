{% extends 'base.twig' %}

{% block title %}{{ user.first_name }} {{ user.last_name }}{% endblock %}

{% block content %}
    <h4>{{ user.first_name }} {{ user.last_name }}</h4>
    <a href="{{ path_for('user.show') }}">Back</a>
{% endblock %}