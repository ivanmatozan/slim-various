{% extends 'base.twig' %}

{% block title %}Users{% endblock %}

{% block content %}
    <h4>Users</h4>

    <ul>
        {% for user in users %}
            <li><a href="{{ path_for('user.profile', {id: user.id}) }}">{{ user.username }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}